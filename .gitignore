.venv
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.log
artifacts/models/
artifacts/logs/
artifacts/plots/
artifacts/coverage/
# v5 sweep outputs: per-cell directories + matrix summary CSVs. Large
# binary .pt files should not be tracked; summaries are regenerable.
artifacts/v5_sweep/
# Auto-generated paper-result aggregations (regenerated by aggregate_v5_results.py)
artifacts/RESULTS_V5.md
!artifacts/.gitkeep
.ipynb_checkpoints/
.DS_Store
*.pkl
*.keras
*.h5

# raw ColO-RAN dataset (8.9 GB, do not commit).
# No trailing slash — this path is a symlink in fl-oran-tmc, not a real directory,
# and gitignore patterns ending in '/' do not match symlinks.
raw

# processed parquet files (hundreds of MB) — same symlink caveat as raw above.
data
